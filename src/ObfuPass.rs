/**
 * Filename: ObfuPass.rs
 * Description: Automatically change a users password every five minutes, outputting a new encrypted password to file.
 * Author: Hayden Hartman
 */
use std::process::{Command};
extern crate whoami;


// Function to change password
// Function to encrypt password
// Function to output the encrypted password to a file

fn changePassword() -> String {

    // let output = Command::new("net")
    //     .arg("user")
    //     .arg(whoami::username())
    //     .arg("a")
    //     .spawn()
    //     .map_err(|e| format!("Failed to execute command: {}", e))?;
    
}

